# Nginx 管理控制台 - 产品设计文档

## 1. 产品概述

### 1.1 产品定位
Nginx 管理控制台是一个基于 Web 的 Nginx 配置管理和监控系统，支持分布式管理多台 Nginx 服务器，提供可视化的配置文件管理、日志分析、性能监控和用户权限管理功能。

### 1.2 核心价值
- **集中化管理**：统一管理多台本地和远程 Nginx 服务器
- **可视化操作**：通过 Web 界面进行配置管理，无需 SSH 登录
- **实时监控**：实时查看 Nginx 状态、日志和性能指标
- **权限控制**：基于角色的细粒度权限管理
- **历史追溯**：配置变更历史记录和回滚功能

## 2. 技术架构

### 2.1 整体架构
```
┌─────────────────────────────────────────────────────────┐
│                     前端 (React)                        │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐ │
│  │ Dashboard│  │ Configs  │  │  Logs    │  │  Stats   │ │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘ │
└─────────────────────────────────────────────────────────┘
                            ↓ HTTP/REST API
┌─────────────────────────────────────────────────────────┐
│                   后端 (Node.js + Express)              │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐ │
│  │   Auth   │  │  Config  │  │   Log    │  │  Stats   │ │
│  │  Middleware│ │  Routes  │  │  Routes  │  │  Routes  │ │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘ │
│  ┌──────────────────────────────────────────────────┐   │
│  │            SQLite Database (better-sqlite3)     │   │
│  └──────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
                            ↓ SSH2
┌─────────────────────────────────────────────────────────┐
│              Nginx 服务器 (本地/远程)                    │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐              │
│  │  Config  │  │   Logs   │  │  Status  │              │
│  └──────────┘  └──────────┘  └──────────┘              │
└─────────────────────────────────────────────────────────┘
```

### 2.2 技术栈

#### 前端技术栈
- **框架**: React 18
- **UI 组件库**: Ant Design 5.x
- **路由**: React Router 6
- **HTTP 客户端**: Axios
- **代码编辑器**: Monaco Editor
- **状态管理**: React Hooks (Context API)
- **构建工具**: Create React App

#### 后端技术栈
- **运行时**: Node.js 16+
- **Web 框架**: Express.js
- **数据库**: SQLite (better-sqlite3)
- **认证**: JWT (jsonwebtoken)
- **密码加密**: bcryptjs
- **SSH 连接**: SSH2
- **文件操作**: fs-extra
- **文件匹配**: glob
- **安全**: Helmet, CORS
- **输入验证**: Zod
- **速率限制**: Express Rate Limit
- **响应压缩**: Compression

#### 部署技术栈
- **容器化**: Docker & Docker Compose
- **反向代理**: Nginx
- **数据持久化**: Docker Volume

### 2.3 性能优化架构

#### 后端优化
- **数据库索引**: 为所有表添加索引，查询性能提升 70-90%
- **输入验证**: 使用 Zod 进行完整的输入验证，防止无效输入
- **错误处理**: 统一的错误处理中间件和自定义错误类
- **速率限制**: 防止 API 滥用，保护服务器资源
- **安全增强**: Helmet 安全头、CORS 配置、响应压缩
- **控制器层**: 分离业务逻辑，提升代码可维护性
- **服务层**: 封装业务逻辑，便于复用和测试
- **API 响应格式统一**: 统一所有 API 响应格式为 `{ success: true, data: ... }`

#### 前端优化
- **TypeScript 类型定义**: 完整的类型定义，提升代码质量和开发体验
- **可复用 UI 组件**: 创建 5 个通用组件（DataTable、ModalForm、PageHeader、StatCard、ServerSelector）
- **代码分割和懒加载**: 使用 React.lazy 和 Suspense，首屏加载时间减少 50-60%
- **React Query 集成**: 服务端状态管理，自动缓存和重新验证
- **组件性能优化**: 使用 React.memo、useMemo、useCallback 优化渲染性能
- **API 数据访问修复**: 统一使用 `response.data?.data` 访问数据，修复页面空白问题

#### 性能提升数据

| 指标 | 优化前 | 优化后 | 提升 |
|------|--------|--------|------|
| 数据库查询时间 | 100-300ms | 10-50ms | 70-90% |
| 首屏加载时间 | 3-5s | 1-2s | 50-60% |
| Bundle 大小 | 2-3MB | 500KB-1MB | 60-70% |
| 页面空白问题 | 有 | 无 | ✅ |

详细的优化报告请查看：
- [优化完成报告](./OPTIMIZATION_COMPLETED.md)
- [优化实施报告](./OPTIMIZATION_REPORT.md)

## 3. 功能模块设计

### 3.1 仪表盘 (Dashboard)

**功能描述**：
- 系统概览和关键指标展示
- 24 小时请求趋势图
- 状态码分布统计
- 请求方法分布
- 多服务器切换查看
- 实时数据更新

**关键指标**：
- 总请求数
- 成功率
- 错误率
- 独立访客数
- 活跃连接数
- 平均响应时间

**技术实现**：
- 前端：[Dashboard.js](file:///f:/Study/nginx-admin-console/frontend/src/pages/Dashboard.js)
- 数据来源：配置统计、日志统计、Nginx 状态 API

### 3.2 配置文件管理 (Config Files)

**功能描述**：
- 查看、编辑、创建、删除 Nginx 配置文件
- 支持本地和远程服务器管理
- 支持 .conf 和 .stream 配置文件
- 配置验证和应用功能
- Monaco Editor 代码编辑器
- 启用/禁用配置文件

**核心功能**：
1. **配置文件列表**
   - 显示所有配置文件
   - 支持按服务器过滤
   - 显示文件状态（启用/禁用）

2. **配置编辑**
   - Monaco Editor 代码编辑器
   - 语法高亮
   - 代码自动补全
   - 配置验证

3. **配置操作**
   - 创建新配置文件
   - 更新现有配置
   - 删除配置文件
   - 启用/禁用配置
   - 应用配置（重载 Nginx）

**技术实现**：
- 前端：[ConfigFiles.js](file:///f:/Study/nginx-admin-console/frontend/src/pages/ConfigFiles.js)
- 后端：[configs.js](file:///f:/Study/nginx-admin-console/backend/src/routes/configs.js)
- API 路由：`/api/configs/*`

**关键 API**：
- `GET /api/configs` - 获取配置文件列表
- `GET /api/configs/content` - 获取配置文件内容
- `POST /api/configs` - 创建配置文件
- `PUT /api/configs/:path` - 更新配置文件
- `DELETE /api/configs/:path` - 删除配置文件
- `POST /api/configs/validate` - 验证配置
- `POST /api/configs/apply` - 应用配置

### 3.3 日志查看 (Logs)

**功能描述**：
- 实时查看访问日志和错误日志
- 支持本地和远程服务器管理
- 按配置文件过滤
- 关键字搜索功能（IP、请求路径等）
- 高性能日志解析
- 最新日志置顶显示
- 长日志条目折叠功能
- 日志格式模板系统（支持自定义格式）
- 自定义列功能（从日志中提取自定义字段）
- 双视图模式（列视图/原始视图）
- 自动统计日志条目总数

**日志格式支持**：
1. **Nginx Default Log Format**
   - 格式：`$remote_addr - $remote_user [$time_local] "$request" $status $body_bytes_sent "$http_referer" "$http_user_agent"`
   - 字段：IP、时间、请求方法、路径、协议、状态码、响应大小、来源、用户代理

2. **Combined with Virtual Host**
   - 格式：`$host $remote_addr - $remote_user [$time_local] "$request" $status $body_bytes_sent "$http_referer" "$http_user_agent"`
   - 字段：虚拟主机、IP、时间、请求方法、路径、协议、状态码、响应大小、来源、用户代理

3. **Common Log Format**
   - 格式：`$remote_addr - $remote_user [$time_local] "$request"`
   - 字段：IP、时间、请求方法、路径、协议

4. **JSON Log Format**
   - 格式：JSON 格式日志
   - 字段：根据 JSON 结构动态解析

5. **Custom App Log Format**
   - 格式：自定义应用日志格式
   - 字段：根据自定义正则表达式提取

**自定义列功能**：
- 支持添加自定义列
- 使用正则表达式提取字段值
- 可配置列宽和最小宽度
- 支持弹性布局
- 列配置保存在浏览器本地存储

**技术实现**：
- 前端：[Logs.js](file:///f:/Study/nginx-admin-console/frontend/src/pages/Logs.js)
- 后端：[logs.js](file:///f:/Study/nginx-admin-console/backend/src/routes/logs.js)
- API 路由：`/api/logs/*`

**关键 API**：
- `GET /api/logs/files` - 获取日志文件列表
- `GET /api/logs/access` - 获取访问日志
- `GET /api/logs/error` - 获取错误日志
- `GET /api/logs/trend` - 获取日志趋势

### 3.4 日志统计 (Log Statistics)

**功能描述**：
- 基于访问日志的统计分析
- 总请求数、独立访客数、总流量统计
- 成功率、错误率分析
- 状态码分布（200、4xx、5xx）
- 请求方法分布（GET、POST、PUT、DELETE等）
- 热门路径 Top 10
- 热门 IP Top 10
- 访问趋势分析（按天统计）
- 支持多时间范围查询

**统计指标**：
1. **基础统计**
   - 总请求数
   - 独立访客数
   - 总流量（字节数）
   - 成功率
   - 错误率

2. **状态码分布**
   - 2xx 成功
   - 3xx 重定向
   - 4xx 客户端错误
   - 5xx 服务器错误

3. **请求方法分布**
   - GET
   - POST
   - PUT
   - DELETE
   - PATCH
   - HEAD
   - OPTIONS

4. **热门内容**
   - 热门路径 Top 10
   - 热门 IP Top 10

5. **趋势分析**
   - 按天统计
   - 按小时统计
   - 可配置时间范围

**技术实现**：
- 前端：[LogStatistics.js](file:///f:/Study/nginx-admin-console/frontend/src/pages/LogStatistics.js)
- 后端：[log-statistics.js](file:///f:/Study/nginx-admin-console/backend/src/routes/log-statistics.js)
- API 路由：`/api/log-statistics/*`

**关键 API**：
- `GET /api/log-statistics/statistics` - 获取日志统计信息
- `GET /api/log-statistics/trends` - 获取日志趋势数据

### 3.5 Upstream 管理 (Upstreams)

**功能描述**：
- 监控和管理 Nginx Upstream 配置
- 实时显示后端服务器状态
- 健康状态监控
- 点击配置文件直接跳转编辑
- 支持远程服务器管理
- 负载均衡算法选择
- 备份服务器管理

**负载均衡算法**：
- 轮询（Round Robin）
- 最少连接（Least Connections）
- IP 哈希（IP Hash）
- 随机（Random）

**技术实现**：
- 前端：[Upstreams.js](file:///f:/Study/nginx-admin-console/frontend/src/pages/Upstreams.js)
- 后端：[upstreams.js](file:///f:/Study/nginx-admin-console/backend/src/routes/upstreams.js)
- API 路由：`/api/upstreams/*`

**关键 API**：
- `GET /api/upstreams` - 获取 Upstream 列表
- `GET /api/upstreams/stats` - 获取 Upstream 统计信息

### 3.6 性能统计 (Stats)

**功能描述**：
- Nginx stub_status 实时监控
- 活跃连接数
- 总请求数、已处理数、已接受数
- 正在读取、写入、等待的连接数
- 请求速率统计
- 历史数据记录和趋势分析
- 支持多服务器监控
- 可配置 nginx_status_url

**监控指标**：
1. **连接统计**
   - 活跃连接数
   - 正在读取的连接数
   - 正在写入的连接数
   - 等待的连接数

2. **请求统计**
   - 已接受的连接数
   - 已处理的连接数
   - 总请求数

3. **历史数据**
   - 按时间记录统计数据
   - 趋势图表展示
   - 支持数据导出

**技术实现**：
- 前端：[Stats.js](file:///f:/Study/nginx-admin-console/frontend/src/pages/Stats.js)
- 后端：[stats.js](file:///f:/Study/nginx-admin-console/backend/src/routes/stats.js)
- API 路由：`/api/stats/*`

**关键 API**：
- `GET /api/stats` - 获取 Nginx 统计信息
- `GET /api/stats/history` - 获取统计历史
- `POST /api/stats/record` - 记录统计数据

### 3.7 服务器管理 (Servers)

**功能描述**：
- 分布式管理多台 Nginx 服务器
- 添加、编辑、删除服务器
- SSH 连接测试
- 支持密码和私钥认证
- 本地服务器默认配置
- 支持 sudo 权限委派
- 可配置 nginx 配置路径、日志路径、状态地址

**服务器配置**：
- 主机地址
- SSH 端口
- 用户名
- 认证方式（密码/私钥）
- 使用 sudo（可选）
- Nginx 配置路径
- Nginx 日志路径
- Nginx 状态 URL

**技术实现**：
- 前端：[Servers.js](file:///f:/Study/nginx-admin-console/frontend/src/pages/Servers.js)
- 后端：[servers.js](file:///f:/Study/nginx-admin-console/backend/src/routes/servers.js), [servers-crud.js](file:///f:/Study/nginx-admin-console/backend/src/routes/servers-crud.js)
- API 路由：`/api/servers/*`

**关键 API**：
- `GET /api/servers` - 获取服务器列表
- `GET /api/servers/:id` - 获取服务器详情
- `POST /api/servers` - 创建服务器
- `PUT /api/servers/:id` - 更新服务器
- `DELETE /api/servers/:id` - 删除服务器
- `POST /api/servers/test-connection` - 测试 SSH 连接
- `POST /api/servers/reload-nginx` - 重载远程 Nginx
- `GET /api/servers/nginx-status` - 获取远程 Nginx 状态

### 3.8 历史记录 (History)

**功能描述**：
- 配置文件变更历史记录
- 支持回滚到历史版本
- 东八时区时间显示
- 操作记录和内容恢复
- 历史版本对比

**历史记录信息**：
- 配置文件路径
- 操作类型（创建、更新、删除）
- 操作人
- 操作时间
- 配置内容
- 备注信息

**技术实现**：
- 前端：[History.js](file:///f:/Study/nginx-admin-console/frontend/src/pages/History.js)
- 后端：[history.js](file:///f:/Study/nginx-admin-console/backend/src/routes/history.js)
- API 路由：`/api/history/*`

**关键 API**：
- `GET /api/history` - 获取历史记录列表
- `GET /api/history/:id` - 获取历史记录详情
- `POST /api/history/:id/restore` - 恢复历史版本

### 3.9 用户管理 (Users)

**功能描述**：
- 用户账号管理
- 创建、编辑、删除用户
- 创建时间记录
- 最后登录时间跟踪
- 用户状态管理（启用/禁用）
- 密码修改

**用户信息**：
- 用户名
- 邮箱
- 密码（加密存储）
- 角色
- 状态
- 创建时间
- 最后登录时间

**技术实现**：
- 前端：[Users.js](file:///f:/Study/nginx-admin-console/frontend/src/pages/Users.js)
- 后端：[users.js](file:///f:/Study/nginx-admin-console/backend/src/routes/users.js)
- API 路由：`/api/users/*`

**关键 API**：
- `GET /api/users` - 获取用户列表
- `GET /api/users/:id` - 获取用户详情
- `POST /api/users` - 创建用户
- `PUT /api/users/:id` - 更新用户
- `DELETE /api/users/:id` - 删除用户
- `GET /api/users/profile/me` - 获取当前用户信息
- `PUT /api/users/profile/me` - 更新当前用户信息
- `PUT /api/users/profile/me/password` - 修改密码

### 3.10 角色权限管理 (Roles)

**功能描述**：
- 基于角色的权限管理系统（RBAC）
- 细粒度权限控制
- 支持自定义角色
- 菜单根据权限动态显示
- 完整的权限体系

**预定义角色**：
1. **管理员**
   - 所有权限
   - 系统管理
   - 用户和角色管理

2. **开发者**
   - 配置读写和应用
   - Upstream 管理
   - 日志查看
   - 历史查看
   - 统计查看
   - 服务器查看

3. **查看者**
   - 配置查看
   - Upstream 查看
   - 日志查看
   - 历史查看
   - 统计查看
   - 服务器查看

**权限列表**：
- `config:read` - 查看配置
- `config:write` - 编辑配置
- `config:delete` - 删除配置
- `config:apply` - 应用配置
- `upstream:read` - 查看 Upstream
- `upstream:manage` - 管理 Upstream
- `log:read` - 查看日志
- `log:statistics` - 日志统计
- `history:read` - 查看历史
- `history:restore` - 恢复历史
- `stats:read` - 查看统计
- `user:manage` - 用户管理
- `role:manage` - 角色管理
- `server:read` - 查看服务器
- `server:manage` - 管理服务器
- `system:manage` - 系统设置

**技术实现**：
- 前端：[Roles.js](file:///f:/Study/nginx-admin-console/frontend/src/pages/Roles.js)
- 后端：[roles.js](file:///f:/Study/nginx-admin-console/backend/src/routes/roles.js)
- 中间件：[auth.js](file:///f:/Study/nginx-admin-console/backend/src/middleware/auth.js)
- API 路由：`/api/roles/*`

**关键 API**：
- `GET /api/roles` - 获取角色列表
- `GET /api/roles/:id` - 获取角色详情
- `GET /api/roles/permissions` - 获取权限列表
- `POST /api/roles` - 创建角色
- `PUT /api/roles/:id` - 更新角色
- `DELETE /api/roles/:id` - 删除角色

### 3.11 系统设置 (Settings)

**功能描述**：
- 配置 Nginx 路径、日志路径、nginx_status_url 等系统参数
- 系统信息查看
- 路径测试功能

**系统设置项**：
- Nginx 配置路径
- Nginx 日志路径
- 最大日志行数
- 自动刷新间隔
- 启用历史记录
- 历史记录保留天数
- Nginx 状态 URL

**技术实现**：
- 前端：[Settings.js](file:///f:/Study/nginx-admin-console/frontend/src/pages/Settings.js)
- 后端：[settings.js](file:///f:/Study/nginx-admin-console/backend/src/routes/settings.js)
- API 路由：`/api/settings/*`

**关键 API**：
- `GET /api/settings` - 获取系统设置
- `PUT /api/settings` - 更新系统设置
- `POST /api/settings/test` - 测试路径
- `GET /api/settings/info` - 获取系统信息

### 3.12 用户认证 (Auth)

**功能描述**：
- 用户登录/登出
- JWT Token 认证
- 密码加密存储
- 会话管理

**技术实现**：
- 前端：[Login.js](file:///f:/Study/nginx-admin-console/frontend/src/pages/Login.js)
- 后端：[auth.js](file:///f:/Study/nginx-admin-console/backend/src/routes/auth.js)
- 工具：[auth.js](file:///f:/Study/nginx-admin-console/frontend/src/utils/auth.js)
- API 路由：`/api/auth/*`

**关键 API**：
- `POST /api/auth/login` - 用户登录
- `POST /api/auth/logout` - 用户登出
- `GET /api/auth/me` - 获取当前用户信息

## 4. 数据库设计

### 4.1 数据库表结构

#### users 表
```sql
CREATE TABLE users (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  username TEXT UNIQUE NOT NULL,
  email TEXT UNIQUE NOT NULL,
  password TEXT NOT NULL,
  role TEXT NOT NULL,
  status INTEGER DEFAULT 1,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  last_login_at DATETIME
);
```

**索引**：
- `idx_users_username` - 用户名索引
- `idx_users_email` - 邮箱索引

#### roles 表
```sql
CREATE TABLE roles (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  name TEXT UNIQUE NOT NULL,
  description TEXT NOT NULL,
  permissions TEXT NOT NULL,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

#### servers 表
```sql
CREATE TABLE servers (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  name TEXT NOT NULL,
  host TEXT NOT NULL,
  port INTEGER DEFAULT 22,
  username TEXT NOT NULL,
  password TEXT,
  private_key TEXT,
  use_sudo INTEGER DEFAULT 0,
  is_default INTEGER DEFAULT 0,
  nginx_config_path TEXT,
  nginx_log_path TEXT,
  nginx_status_url TEXT,
  status INTEGER DEFAULT 1,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

#### config_history 表
```sql
CREATE TABLE config_history (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  config_path TEXT NOT NULL,
  action TEXT NOT NULL,
  operator TEXT NOT NULL,
  content TEXT,
  comment TEXT,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

**索引**：
- `idx_config_history_path` - 配置路径索引
- `idx_config_history_created` - 创建时间索引

#### settings 表
```sql
CREATE TABLE settings (
  key TEXT PRIMARY KEY,
  value TEXT NOT NULL,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

#### stats 表
```sql
CREATE TABLE stats (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  server_id INTEGER,
  active_connections INTEGER,
  accepts INTEGER,
  handled INTEGER,
  requests INTEGER,
  reading INTEGER,
  writing INTEGER,
  waiting INTEGER,
  recorded_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

### 4.2 默认数据

#### 默认角色
1. **admin** - 管理员
   - 所有权限

2. **developer** - 开发者
   - 配置读写和应用
   - Upstream 管理
   - 日志查看
   - 历史查看
   - 统计查看
   - 服务器查看

3. **viewer** - 查看者
   - 配置查看
   - Upstream 查看
   - 日志查看
   - 历史查看
   - 统计查看
   - 服务器查看

#### 默认用户
- 用户名：`admin`
- 密码：`admin123`
- 角色：`admin`

#### 默认设置
- `nginx_config_path`: `/etc/nginx`
- `nginx_log_path`: `/var/log/nginx`
- `max_log_lines`: `1000`
- `auto_refresh_interval`: `5`
- `enable_history`: `1`
- `history_retention_days`: `30`
- `nginx_status_url`: `http://localhost/nginx_status`

## 5. API 设计

### 5.1 认证 API

#### 登录
```
POST /api/auth/login
Content-Type: application/json

{
  "username": "admin",
  "password": "admin123"
}

Response:
{
  "token": "jwt_token_here",
  "user": {
    "id": 1,
    "username": "admin",
    "email": "admin@example.com",
    "role": "admin"
  }
}
```

#### 登出
```
POST /api/auth/logout
Authorization: Bearer {token}

Response:
{
  "message": "登出成功"
}
```

#### 获取当前用户信息
```
GET /api/auth/me
Authorization: Bearer {token}

Response:
{
  "id": 1,
  "username": "admin",
  "email": "admin@example.com",
  "role": "admin",
  "permissions": [...]
}
```

### 5.2 配置文件 API

#### 获取配置文件列表
```
GET /api/configs?serverId=1
Authorization: Bearer {token}

Response:
{
  "configs": [
    {
      "name": "default.conf",
      "path": "/etc/nginx/conf.d/default.conf",
      "enabled": true,
      "size": 1024,
      "modified": "2024-01-01 12:00:00"
    }
  ]
}
```

#### 获取配置文件内容
```
GET /api/configs/content?path=/etc/nginx/conf.d/default.conf&serverId=1
Authorization: Bearer {token}

Response:
{
  "content": "server {\n    listen 80;\n    ...\n}",
  "path": "/etc/nginx/conf.d/default.conf"
}
```

#### 创建配置文件
```
POST /api/configs
Authorization: Bearer {token}
Content-Type: application/json

{
  "path": "/etc/nginx/conf.d/new.conf",
  "content": "server {\n    listen 80;\n    ...\n}",
  "serverId": 1
}

Response:
{
  "message": "配置文件创建成功",
  "path": "/etc/nginx/conf.d/new.conf"
}
```

#### 更新配置文件
```
PUT /api/configs/encodeURIComponent(path)
Authorization: Bearer {token}
Content-Type: application/json

{
  "content": "server {\n    listen 80;\n    ...\n}",
  "serverId": 1
}

Response:
{
  "message": "配置文件更新成功"
}
```

#### 删除配置文件
```
DELETE /api/configs/encodeURIComponent(path)?serverId=1
Authorization: Bearer {token}

Response:
{
  "message": "配置文件删除成功"
}
```

#### 验证配置
```
POST /api/configs/validate
Authorization: Bearer {token}
Content-Type: application/json

{
  "content": "server {\n    listen 80;\n    ...\n}",
  "serverId": 1
}

Response:
{
  "valid": true,
  "message": "配置验证通过"
}
```

#### 应用配置
```
POST /api/configs/apply
Authorization: Bearer {token}
Content-Type: application/json

{
  "serverId": 1
}

Response:
{
  "message": "Nginx 重载成功"
}
```

### 5.3 日志 API

#### 获取日志文件列表
```
GET /api/logs/files?serverId=1
Authorization: Bearer {token}

Response:
{
  "files": [
    {
      "name": "access.log",
      "path": "/var/log/nginx/access.log",
      "size": 1024000,
      "modified": "2024-01-01 12:00:00"
    },
    {
      "name": "error.log",
      "path": "/var/log/nginx/error.log",
      "size": 51200,
      "modified": "2024-01-01 12:00:00"
    }
  ]
}
```

#### 获取访问日志
```
GET /api/logs/access?file=access.log&lines=100&keyword=192.168&serverId=1
Authorization: Bearer {token}

Response:
{
  "logs": [
    {
      "ip": "192.168.1.1",
      "time": "01/Jan/2024:12:00:00 +0800",
      "method": "GET",
      "path": "/api/users",
      "protocol": "HTTP/1.1",
      "status": 200,
      "size": 1234,
      "referer": "-",
      "userAgent": "Mozilla/5.0..."
    }
  ],
  "total": 1000,
  "success": 950,
  "error": 50
}
```

#### 获取错误日志
```
GET /api/logs/error?file=error.log&lines=100&keyword=error&serverId=1
Authorization: Bearer {token}

Response:
{
  "logs": [
    {
      "time": "2024/01/01 12:00:00",
      "level": "error",
      "pid": 1234,
      "tid": 5678,
      "message": "Connection refused"
    }
  ],
  "total": 100
}
```

### 5.4 日志统计 API

#### 获取日志统计信息
```
GET /api/log-statistics/statistics?serverId=1&hours=24
Authorization: Bearer {token}

Response:
{
  "totalRequests": 10000,
  "uniqueVisitors": 5000,
  "totalBytes": 1024000000,
  "successRate": 95.5,
  "errorRate": 4.5,
  "statusCodeDistribution": {
    "2xx": 9500,
    "3xx": 200,
    "4xx": 250,
    "5xx": 50
  },
  "methodDistribution": {
    "GET": 8000,
    "POST": 1500,
    "PUT": 300,
    "DELETE": 200
  },
  "topPaths": [
    { "path": "/api/users", "count": 2000 },
    { "path": "/api/configs", "count": 1500 }
  ],
  "topIPs": [
    { "ip": "192.168.1.1", "count": 500 },
    { "ip": "192.168.1.2", "count": 400 }
  ]
}
```

#### 获取日志趋势数据
```
GET /api/log-statistics/trends?serverId=1&days=7
Authorization: Bearer {token}

Response:
{
  "trends": [
    {
      "date": "2024-01-01",
      "requests": 10000,
      "uniqueVisitors": 5000,
      "successRate": 95.5
    },
    {
      "date": "2024-01-02",
      "requests": 12000,
      "uniqueVisitors": 6000,
      "successRate": 96.0
    }
  ]
}
```

### 5.5 Upstream API

#### 获取 Upstream 列表
```
GET /api/upstreams?serverId=1
Authorization: Bearer {token}

Response:
{
  "upstreams": [
    {
      "name": "backend",
      "configFile": "/etc/nginx/conf.d/backend.conf",
      "algorithm": "least_conn",
      "servers": [
        {
          "address": "192.168.1.10:8080",
          "weight": 1,
          "maxFails": 3,
          "failTimeout": "30s",
          "backup": false,
          "down": false
        }
      ]
    }
  ]
}
```

#### 获取 Upstream 统计信息
```
GET /api/upstreams/stats?serverId=1
Authorization: Bearer {token}

Response:
{
  "stats": [
    {
      "upstream": "backend",
      "server": "192.168.1.10:8080",
      "requests": 10000,
      "responses": 9500,
      "fails": 500,
      "unavail": 0,
      "healthChecks": {
        "passes": 9500,
        "fails": 500,
        "unhealthy": false
      }
    }
  ]
}
```

### 5.6 性能统计 API

#### 获取 Nginx 统计信息
```
GET /api/stats?serverId=1
Authorization: Bearer {token}

Response:
{
  "activeConnections": 100,
  "accepts": 1000000,
  "handled": 999000,
  "requests": 5000000,
  "reading": 10,
  "writing": 20,
  "waiting": 70
}
```

#### 获取统计历史
```
GET /api/stats/history?serverId=1
Authorization: Bearer {token}

Response:
{
  "history": [
    {
      "id": 1,
      "serverId": 1,
      "activeConnections": 100,
      "accepts": 1000000,
      "handled": 999000,
      "requests": 5000000,
      "reading": 10,
      "writing": 20,
      "waiting": 70,
      "recordedAt": "2024-01-01 12:00:00"
    }
  ]
}
```

#### 记录统计数据
```
POST /api/stats/record?serverId=1
Authorization: Bearer {token}

Response:
{
  "message": "统计数据记录成功",
  "id": 1
}
```

### 5.7 服务器 API

#### 获取服务器列表
```
GET /api/servers
Authorization: Bearer {token}

Response:
{
  "servers": [
    {
      "id": 1,
      "name": "Local Server",
      "host": "localhost",
      "port": 22,
      "username": "nginx",
      "useSudo": false,
      "isDefault": true,
      "nginxConfigPath": "/etc/nginx",
      "nginxLogPath": "/var/log/nginx",
      "nginxStatusUrl": "http://localhost/nginx_status",
      "status": 1,
      "createdAt": "2024-01-01 12:00:00"
    }
  ]
}
```

#### 创建服务器
```
POST /api/servers
Authorization: Bearer {token}
Content-Type: application/json

{
  "name": "Remote Server",
  "host": "192.168.1.100",
  "port": 22,
  "username": "nginx",
  "password": "password",
  "useSudo": false,
  "nginxConfigPath": "/etc/nginx",
  "nginxLogPath": "/var/log/nginx",
  "nginxStatusUrl": "http://192.168.1.100/nginx_status"
}

Response:
{
  "message": "服务器创建成功",
  "id": 2
}
```

#### 测试 SSH 连接
```
POST /api/servers/test-connection
Authorization: Bearer {token}
Content-Type: application/json

{
  "host": "192.168.1.100",
  "port": 22,
  "username": "nginx",
  "password": "password"
}

Response:
{
  "success": true,
  "message": "连接成功"
}
```

### 5.8 用户 API

#### 获取用户列表
```
GET /api/users
Authorization: Bearer {token}

Response:
{
  "users": [
    {
      "id": 1,
      "username": "admin",
      "email": "admin@example.com",
      "role": "admin",
      "status": 1,
      "createdAt": "2024-01-01 12:00:00",
      "lastLoginAt": "2024-01-01 12:00:00"
    }
  ]
}
```

#### 创建用户
```
POST /api/users
Authorization: Bearer {token}
Content-Type: application/json

{
  "username": "developer",
  "email": "developer@example.com",
  "password": "password123",
  "role": "developer"
}

Response:
{
  "message": "用户创建成功",
  "id": 2
}
```

### 5.9 角色权限 API

#### 获取角色列表
```
GET /api/roles
Authorization: Bearer {token}

Response:
{
  "roles": [
    {
      "id": 1,
      "name": "admin",
      "description": "管理员",
      "permissions": [
        "config:read",
        "config:write",
        ...
      ]
    }
  ]
}
```

#### 创建角色
```
POST /api/roles
Authorization: Bearer {token}
Content-Type: application/json

{
  "name": "operator",
  "description": "操作员",
  "permissions": [
    "config:read",
    "config:write",
    "log:read"
  ]
}

Response:
{
  "message": "角色创建成功",
  "id": 4
}
```

### 5.10 历史记录 API

#### 获取历史记录列表
```
GET /api/history
Authorization: Bearer {token}

Response:
{
  "history": [
    {
      "id": 1,
      "configPath": "/etc/nginx/conf.d/default.conf",
      "action": "update",
      "operator": "admin",
      "comment": "更新配置",
      "createdAt": "2024-01-01 12:00:00"
    }
  ]
}
```

#### 恢复历史版本
```
POST /api/history/1/restore
Authorization: Bearer {token}

Response:
{
  "message": "历史版本恢复成功"
}
```

### 5.11 系统设置 API

#### 获取系统设置
```
GET /api/settings
Authorization: Bearer {token}

Response:
{
  "settings": {
    "nginxConfigPath": "/etc/nginx",
    "nginxLogPath": "/var/log/nginx",
    "maxLogLines": 1000,
    "autoRefreshInterval": 5,
    "enableHistory": true,
    "historyRetentionDays": 30,
    "nginxStatusUrl": "http://localhost/nginx_status"
  }
}
```

#### 更新系统设置
```
PUT /api/settings
Authorization: Bearer {token}
Content-Type: application/json

{
  "nginxConfigPath": "/etc/nginx",
  "nginxLogPath": "/var/log/nginx",
  "maxLogLines": 2000,
  "autoRefreshInterval": 10
}

Response:
{
  "message": "系统设置更新成功"
}
```

## 6. 安全设计

### 6.1 认证与授权

#### JWT Token 认证
- 使用 JWT (JSON Web Token) 进行用户认证
- Token 存储在 localStorage 中
- 每次请求在 Authorization header 中携带 Token
- Token 过期时间：24 小时（可配置）

#### 密码加密
- 使用 bcryptjs 进行密码加密
- 加密强度：10 rounds
- 密码不以明文形式存储

#### 权限控制
- 基于角色的访问控制（RBAC）
- 细粒度权限控制
- 路由级别的权限验证
- 菜单根据权限动态显示

### 6.2 API 安全

#### 安全中间件
- **Helmet**: 设置安全相关的 HTTP headers
- **CORS**: 配置跨域资源共享
- **Rate Limiting**: 防止 API 滥用（待实现）

#### 请求验证
- 所有 API 请求需要携带有效的 JWT Token
- 敏感操作需要特定权限
- 输入参数验证

### 6.3 数据安全

#### 数据库安全
- SQLite 数据库文件权限控制
- 敏感数据加密存储
- 定期备份

#### 日志安全
- 不记录敏感信息（密码、Token）
- 日志文件权限控制
- 日志轮转

### 6.4 远程连接安全

#### SSH 连接
- 支持 SSH 密钥认证
- 支持 sudo 权限委派
- 连接超时控制
- 连接错误处理

### 6.5 安全建议

1. **修改默认密码**
   - 首次登录后立即修改 admin 密码
   - 使用强密码策略

2. **保护 JWT_SECRET**
   - 使用强随机字符串作为 JWT_SECRET
   - 不要在代码中硬编码
   - 使用环境变量配置

3. **启用 HTTPS**
   - 生产环境必须使用 HTTPS
   - 配置 SSL/TLS 证书

4. **限制访问**
   - 使用防火墙限制 API 访问
   - 限制 SSH 访问 IP
   - 使用 VPN 或专用网络

5. **定期备份**
   - 定期备份数据库
   - 备份配置文件
   - 备份日志文件

6. **监控和审计**
   - 记录所有操作日志
   - 监控异常访问
   - 定期审计权限

## 7. 部署架构

### 7.1 开发环境

#### 本地开发
```bash
# 后端
cd backend
npm install
npm start

# 前端
cd frontend
npm install
npm start
```

#### 环境变量
```env
# backend/.env
PORT=5000
NODE_ENV=development
JWT_SECRET=your-secret-key
NGINX_CONFIG_PATH=/etc/nginx
NGINX_LOG_PATH=/var/log/nginx
DB_PATH=/tmp/nginx-admin.db
```

### 7.2 生产环境

#### Docker Compose 部署（推荐）
```yaml
version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
      - JWT_SECRET=${JWT_SECRET}
      - NGINX_CONFIG_PATH=/etc/nginx
      - NGINX_LOG_PATH=/var/log/nginx
      - DB_PATH=/app/data/nginx-admin.db
    restart: unless-stopped

  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: unless-stopped
```

#### 手动部署

1. **后端部署**
```bash
cd backend
npm install --production
npm start
```

2. **前端部署**
```bash
cd frontend
npm run build
# 将 build 目录部署到 Web 服务器
```

3. **进程管理**
```bash
# 使用 PM2
pm2 start backend/src/index.js --name nginx-admin-backend

# 使用 systemd
# 创建 /etc/systemd/system/nginx-admin.service
```

### 7.3 反向代理配置

#### Nginx 反向代理
```nginx
server {
    listen 80;
    server_name your-domain.com;

    # 前端静态文件
    location / {
        root /path/to/frontend/build;
        try_files $uri $uri/ /index.html;
    }

    # 后端 API
    location /api {
        proxy_pass http://localhost:5000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

### 7.4 Sudo 配置

如果 SSH 用户没有 root 权限，需要配置 sudoers：

```bash
# 编辑 sudoers
sudo visudo

# 添加以下配置
username ALL=(ALL) NOPASSWD: /usr/sbin/nginx
username ALL=(ALL) NOPASSWD: /bin/cat /etc/nginx/*
username ALL=(ALL) NOPASSWD: /bin/ls /var/log/nginx/*
username ALL=(ALL) NOPASSWD: /usr/bin/curl
```

## 8. 用户界面设计

### 8.1 设计原则

- **简洁性**: 界面简洁明了，操作直观
- **一致性**: 统一的视觉风格和交互模式
- **响应式**: 支持桌面端和移动端
- **可访问性**: 支持键盘导航和屏幕阅读器
- **性能**: 快速响应，流畅动画

### 8.2 主题系统

#### 主题切换
- 支持深色/浅色主题切换
- 自动检测系统主题偏好
- 主题配置保存在 localStorage

#### 主题配置
```javascript
// 浅色主题
const lightTheme = {
  algorithm: theme.defaultAlgorithm,
  token: {
    colorPrimary: '#1890ff',
    borderRadius: 6,
  },
};

// 深色主题
const darkTheme = {
  algorithm: theme.darkAlgorithm,
  token: {
    colorPrimary: '#177ddc',
    borderRadius: 6,
  },
};
```

### 8.3 布局设计

#### 主布局
```
┌─────────────────────────────────────────────────────────┐
│ Header (Logo, User Menu, Theme Toggle)                 │
├────────┬────────────────────────────────────────────────┤
│        │                                                │
│        │                                                │
│ Sidebar│              Content Area                     │
│        │                                                │
│        │                                                │
└────────┴────────────────────────────────────────────────┘
```

#### 侧边栏菜单
- 可折叠/展开
- 根据权限动态显示
- 图标 + 文字
- 分组显示

#### 内容区域
- 面包屑导航
- 页面标题
- 操作按钮
- 数据展示
- 分页

### 8.4 组件设计

#### 通用组件
- **Loading**: 加载状态
- **Error**: 错误提示
- **Layout**: 布局组件

#### 业务组件
- **ConfigEditor**: 配置文件编辑器（Monaco Editor）
- **LogViewer**: 日志查看器
- **StatsCard**: 统计卡片
- **TrendChart**: 趋势图表
- **ServerSelector**: 服务器选择器

### 8.5 交互设计

#### 反馈机制
- 操作成功/失败提示
- 加载状态指示
- 确认对话框（删除等危险操作）

#### 快捷操作
- 键盘快捷键（待实现）
- 批量操作
- 快速搜索

#### 数据刷新
- 自动刷新
- 手动刷新按钮
- 刷新间隔配置

## 9. 性能优化

### 9.1 前端优化

#### 代码分割
- React Router 懒加载
- 按需加载组件

#### 缓存策略
- API 响应缓存
- 静态资源缓存
- localStorage 缓存

#### 渲染优化
- 虚拟滚动（长列表）
- 防抖/节流
- React.memo 优化

#### 资源优化
- 图片压缩
- CSS/JS 压缩
- CDN 加速

### 9.2 后端优化

#### 数据库优化
- 索引优化
- 查询优化
- 连接池（待实现）

#### 日志优化
- 日志轮转
- 异步日志
- 日志级别控制

#### 缓存策略
- Redis 缓存（待实现）
- 内存缓存
- HTTP 缓存

#### 并发处理
- 异步 I/O
- 连接池
- 限流

### 9.3 网络优化

#### API 优化
- 批量请求
- 分页加载
- 数据压缩

#### 远程连接优化
- SSH 连接复用
- 连接池
- 超时控制

## 10. 监控与运维

### 10.1 日志管理

#### 应用日志
- 访问日志
- 错误日志
- 操作日志

#### 日志级别
- DEBUG
- INFO
- WARN
- ERROR

#### 日志轮转
- 按大小轮转
- 按时间轮转
- 保留策略

### 10.2 监控指标

#### 系统指标
- CPU 使用率
- 内存使用率
- 磁盘使用率
- 网络流量

#### 应用指标
- 请求响应时间
- 错误率
- 并发连接数
- API 调用次数

#### 业务指标
- 用户活跃度
- 配置变更次数
- 日志查看次数

### 10.3 告警机制

#### 告警规则
- 错误率超过阈值
- 响应时间过长
- 磁盘空间不足
- SSH 连接失败

#### 告警方式
- 邮件通知
- 短信通知（待实现）
- Webhook（待实现）

### 10.4 备份与恢复

#### 数据备份
- 数据库备份
- 配置文件备份
- 日志备份

#### 恢复策略
- 数据库恢复
- 配置恢复
- 历史版本恢复

## 11. 未来规划

### 11.1 功能增强

#### 配置管理
- 配置模板
- 配置版本对比
- 配置批量操作
- 配置导入/导出

#### 日志分析
- 实时日志流
- 日志告警
- 日志导出
- 日志搜索增强

#### 监控告警
- 自定义监控指标
- 告警规则配置
- 告警通知渠道
- 监控仪表盘

#### 用户体验
- 键盘快捷键
- 批量操作
- 拖拽排序
- 主题自定义

### 11.2 技术升级

#### 架构升级
- 微服务架构
- 消息队列
- 分布式缓存

#### 技术栈升级
- TypeScript
- GraphQL
- WebSocket

#### 性能优化
- 服务端渲染
- 边缘计算
- CDN 加速

### 11.3 生态扩展

#### 插件系统
- 插件开发框架
- 插件市场
- 第三方集成

#### API 开放
- RESTful API
- Webhook
- SDK

#### 多语言支持
- 国际化（i18n）
- 多语言界面

## 12. 总结

Nginx 管理控制台是一个功能完善、架构清晰的 Nginx 管理系统，具有以下特点：

### 核心优势
1. **集中化管理**: 统一管理多台 Nginx 服务器
2. **可视化操作**: 直观的 Web 界面，降低使用门槛
3. **权限控制**: 完善的 RBAC 权限体系
4. **历史追溯**: 配置变更历史记录和回滚
5. **实时监控**: 实时查看 Nginx 状态和日志
6. **分布式支持**: 支持本地和远程服务器管理

### 技术亮点
1. **现代化技术栈**: React 18 + Node.js + SQLite
2. **SSH 远程管理**: 使用 SSH2 实现远程服务器管理
3. **JWT 认证**: 安全的 Token 认证机制
4. **Monaco Editor**: 专业的代码编辑体验
5. **响应式设计**: 支持桌面端和移动端
6. **主题切换**: 深色/浅色主题支持

### 应用场景
1. **运维团队**: 简化 Nginx 配置管理流程
2. **开发团队**: 快速查看和修改 Nginx 配置
3. **DevOps**: 集成到 CI/CD 流程
4. **企业**: 统一管理多台 Nginx 服务器

该系统为 Nginx 管理提供了一个完整、易用、安全的解决方案，适合各种规模的组织使用。
